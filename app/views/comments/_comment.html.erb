<tr>
  <td class="comment">
    <span class="info_comment">Posted <%= time_ago_in_words(comment.created_at) %> ago by 
    	<%= link_to User.find_by_id(comment.user_id).name, user_path(User.find_by_id(comment.user_id))%>
	</span>
	<br/>
    <span class="comment_content">
          	<%= comment.content %>
    </span>
  </td>
</tr>
